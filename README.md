# Template Array Stack
C++ implementation of a fixed-size, template-based array stack to store elements of any type.

## Build and Run

To compile the project, run:
```bash
g++ -IHeaders Sources/main.cpp -o templateArrayStack
```
To execute the program, run:
```bash
.\templateArrayStack.exe
```